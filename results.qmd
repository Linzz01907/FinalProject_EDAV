# Results

```{r}
library(tidyverse)
salary_df <- read.csv("Cleaned Dataset.csv")
```

```{r}
salary_filtered <- salary_df |> 
  select(Agency.Name)
```


```{r}

```

```{r}
str(data)
```

```{r}
unique_values <- unique(data$Agency.Name)
unique_values <- unique(data[["Work.Location.Borough"]])
print(unique_values)
```
